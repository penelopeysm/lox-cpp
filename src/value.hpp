#pragma once

namespace lox {

// NOTE: in C++ `using` is preferred over `typedef` for type aliases. Some
// things are awkward to express with `typedef`. Also, `using` can be used
// with templates (whatever that means).
using Value = double;

} // namespace lox
